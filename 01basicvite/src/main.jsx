import { createRoot } from "react-dom/client";
import App from "./App.jsx";
// NOTE  : ReactDOM.createRoot
// NOTE  : ReactDOM :virtual DOM
createRoot(document.getElementById("root")).render(<App />);
